# setuptools<81: mhcflurry imports pkg_resources, which newer setuptools removed. Found during
# real deployment (docs/CORRECTION_PLAN.md §9.1) — mhcflurry install otherwise fails outright.
setuptools<81
mhcflurry>=2.1.0
# pandas pinned <2.1: pvactools pins this range itself; letting pip resolve pandas independently
# of pvactools' own install can otherwise land on an incompatible newer pandas.
pandas>=2.0,<2.1
numpy>=1.24.0,<2
pysam>=0.22.0
biopython>=1.81
